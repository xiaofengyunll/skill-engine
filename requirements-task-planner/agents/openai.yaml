interface:
  display_name: "Requirements Task Planner"
  short_description: "Requirement clarity and task planning"
  default_prompt: "Use $requirements-task-planner to clarify requirements and produce an appropriately scoped task plan."

policy:
  allow_implicit_invocation: true
